#include "main.h"

/**
 * add - function that takes two integer inputs and returns the sum of them
 * @a: input integer
 * @b: inputer integer
 * Return: Sum of the two inputs
 */
int add(a, b)
{
	return (a + b);
}
